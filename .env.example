# Google Sheets API Configuration
# Option 1: Use a Service Account (Recommended - more secure)
# Create a service account in Google Cloud Console, download the JSON key, and paste the entire JSON as a single line
# Share your Google Sheets with the service account email (usually ends with @*.iam.gserviceaccount.com)
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project",...}

# Option 2: Use an API Key (simpler but requires sheets to be publicly viewable)
# Get your API key from: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=your_google_api_key_here

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/apis/credentials
# Make sure to add authorized redirect URIs: http://localhost:5000/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Session Secret (generate a random string)
SESSION_SECRET=your_random_session_secret_here

# Admin Emails (comma-separated list of @scalmob.com emails that should be admins)
ADMIN_EMAILS=your.email@scalmob.com,another.admin@scalmob.com

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
